# BUILD-039A implementation note

This branch contains only a scoped browser-access hotfix for Mission Control telemetry.

It should be merged after review, then `orchid-calyx-backend` should be redeployed on Render.
