# Merge gate

Do not merge CALYX-CORE-REBASE-003 until the focused validation workflow and BUILD-088E both execute real job steps and pass on the unchanged PR head. Zero-step GitHub Actions failures are infrastructure failures, not evidence of code correctness.
