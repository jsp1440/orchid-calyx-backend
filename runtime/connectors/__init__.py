"""Connector implementations.

Each connector is a separate module implementing the ConnectorInterface.
The ConnectorRegistry automatically discovers and loads all connectors
from this directory.
"""
