fastapi<0.116
# Imported directly by app/main.py and app/university/learner_auth.py, so it is
# declared rather than relied on as FastAPI's transitive install. The range is
# the one FastAPI 0.115.x already requires (starlette>=0.40.0,<0.47.0), read
# from its own metadata, so this cannot conflict with the pin above.
# Filed by work discovery as #1594.
starlette>=0.40,<0.47
uvicorn
sqlalchemy>=2.0
psycopg[binary]
psycopg2-binary
pydantic
python-multipart
alembic
qrcode
openpyxl==3.1.5
google-api-python-client>=2.100,<3
google-auth>=2.20,<3
pypdf>=5,<7
python-docx>=1.1,<2
requests
certifi
cryptography>=44,<46
httpx
PyYAML>=6
scipy>=1.11
Pillow
