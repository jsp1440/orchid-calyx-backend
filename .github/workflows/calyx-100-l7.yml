name: CALYX-100-L7 Validation

on:
  pull_request:
    paths:
      - 'runtime/calyx_certification/concept_consumers.py'
      - 'tests/test_calyx_100_l7_concept_consumers.py'
      - '.github/workflows/calyx-100-l7.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: ${{ github.workspace }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - run: pip install pytest ruff
      - run: ruff check runtime/calyx_certification/concept_consumers.py tests/test_calyx_100_l7_concept_consumers.py
      - run: pytest -q tests/test_calyx_100_l7_concept_consumers.py
