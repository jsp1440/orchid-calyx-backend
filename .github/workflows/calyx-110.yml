name: CALYX-110 Validation

on:
  pull_request:
    paths:
      - "runtime/calyx_certification/evidence_retention.py"
      - "tests/test_calyx_110_evidence_retention.py"
      - ".github/workflows/calyx-110.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: .
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pytest ruff
      - run: ruff check runtime/calyx_certification/evidence_retention.py tests/test_calyx_110_evidence_retention.py
      - run: pytest -q tests/test_calyx_110_evidence_retention.py
