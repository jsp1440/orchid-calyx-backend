name: CALYX-129 Validation

on:
  pull_request:
    paths:
      - "runtime/calyx_certification/route_contract.py"
      - "tests/test_calyx_129_route_contract.py"
      - ".github/workflows/calyx-129.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: .
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pytest ruff
      - run: ruff check runtime/calyx_certification/route_contract.py tests/test_calyx_129_route_contract.py
      - run: pytest -q tests/test_calyx_129_route_contract.py
