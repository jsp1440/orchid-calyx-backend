name: CALYX-128 Validation

on:
  pull_request:
    paths:
      - "runtime/calyx_certification/mount_durability.py"
      - "tests/test_calyx_128_mount_durability.py"
      - ".github/workflows/calyx-128.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: .
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pytest ruff
      - run: ruff check runtime/calyx_certification/mount_durability.py tests/test_calyx_128_mount_durability.py
      - run: pytest -q tests/test_calyx_128_mount_durability.py
