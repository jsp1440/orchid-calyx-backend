name: CALYX-100-L3 Validation

on:
  pull_request:
    paths:
      - 'runtime/calyx_certification/source_binding.py'
      - 'tests/test_calyx_100_l3_source_binding.py'
      - '.github/workflows/calyx-100-l3.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: ${{ github.workspace }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - run: pip install pytest ruff
      - run: ruff check runtime/calyx_certification/source_binding.py tests/test_calyx_100_l3_source_binding.py
      - run: pytest -q tests/test_calyx_100_l3_source_binding.py
