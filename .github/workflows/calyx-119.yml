name: CALYX-119 Validation

on:
  pull_request:
    paths:
      - "runtime/calyx_certification/owner_decision_lifecycle.py"
      - "tests/test_calyx_119_owner_decision_lifecycle.py"
      - ".github/workflows/calyx-119.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: .
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pytest ruff
      - run: ruff check runtime/calyx_certification/owner_decision_lifecycle.py tests/test_calyx_119_owner_decision_lifecycle.py
      - run: pytest -q tests/test_calyx_119_owner_decision_lifecycle.py
