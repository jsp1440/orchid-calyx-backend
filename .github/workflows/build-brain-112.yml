name: BUILD-BRAIN-112 Validation

on:
  pull_request:
    paths:
      - "app/calyx_orchestrator/review_eligibility.py"
      - "tests/test_calyx_review_eligibility.py"
      - "docs/brain/BUILD-BRAIN-112-REVIEW-ELIGIBILITY.md"
      - ".github/workflows/build-brain-112.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PYTHONPATH: ${{ github.workspace }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest ruff
      - name: Compile
        run: python -m py_compile app/calyx_orchestrator/review_eligibility.py tests/test_calyx_review_eligibility.py
      - name: Ruff
        run: ruff check app/calyx_orchestrator/review_eligibility.py tests/test_calyx_review_eligibility.py
      - name: Focused tests
        run: pytest -q tests/test_calyx_review_eligibility.py
