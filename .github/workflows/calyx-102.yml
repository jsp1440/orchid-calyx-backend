name: CALYX-102 Validation

on:
  pull_request:
    paths:
      - 'runtime/calyx_certification/status_contract.py'
      - 'tests/test_calyx_102_status_contract.py'
      - '.github/workflows/calyx-102.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: ${{ github.workspace }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - run: pip install pytest ruff
      - run: ruff check runtime/calyx_certification/status_contract.py tests/test_calyx_102_status_contract.py
      - run: pytest -q tests/test_calyx_102_status_contract.py
