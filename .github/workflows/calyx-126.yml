name: CALYX-126 Validation

on:
  pull_request:
    paths:
      - "runtime/calyx_certification/environment_fingerprint.py"
      - "tests/test_calyx_126_environment_fingerprint.py"
      - ".github/workflows/calyx-126.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: .
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pytest ruff
      - run: ruff check runtime/calyx_certification/environment_fingerprint.py tests/test_calyx_126_environment_fingerprint.py
      - run: pytest -q tests/test_calyx_126_environment_fingerprint.py
