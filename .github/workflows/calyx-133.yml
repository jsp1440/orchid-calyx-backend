name: CALYX-133 Validation

on:
  pull_request:
    paths:
      - "runtime/calyx_certification/observation_window.py"
      - "tests/test_calyx_133_observation_window.py"
      - ".github/workflows/calyx-133.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: .
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pytest ruff
      - run: ruff check runtime/calyx_certification/observation_window.py tests/test_calyx_133_observation_window.py
      - run: pytest -q tests/test_calyx_133_observation_window.py
