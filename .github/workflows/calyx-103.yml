name: CALYX-103 Validation

on:
  pull_request:
    paths:
      - 'runtime/calyx_certification/preflight_operator_plan.py'
      - 'tests/test_calyx_103_preflight_operator_plan.py'
      - '.github/workflows/calyx-103.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: ${{ github.workspace }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - run: pip install pytest ruff
      - run: ruff check runtime/calyx_certification/preflight_operator_plan.py tests/test_calyx_103_preflight_operator_plan.py
      - run: pytest -q tests/test_calyx_103_preflight_operator_plan.py
