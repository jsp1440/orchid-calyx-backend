name: CALYX-131 Validation

on:
  pull_request:
    paths:
      - "runtime/calyx_certification/queue_drain.py"
      - "tests/test_calyx_131_queue_drain.py"
      - ".github/workflows/calyx-131.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: .
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pytest ruff
      - run: ruff check runtime/calyx_certification/queue_drain.py tests/test_calyx_131_queue_drain.py
      - run: pytest -q tests/test_calyx_131_queue_drain.py
