name: Calyx Conversation Validation

on:
  pull_request:
    paths:
      - 'app/calyx_conversation/**'
      - 'app/routers/calyx_core.py'
      - 'tests/test_calyx_conversation.py'
      - 'tests/test_calyx_file_analysis.py'
      - 'migrations/610_calyx_conversations.sql'
      - '.github/workflows/calyx-conversation-validation.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-calyx-conversation:
    runs-on: ubuntu-24.04
    env:
      PYTHONPATH: .
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: pip install -r requirements.txt pytest ruff
      - name: Compile Calyx analysis slice
        run: python -m py_compile app/calyx_conversation/routes.py app/calyx_conversation/store.py app/calyx_conversation/file_analysis.py app/calyx_conversation/file_routes.py app/routers/calyx_core.py tests/test_calyx_conversation.py tests/test_calyx_file_analysis.py
      - name: Run focused regression tests
        run: python -m pytest -q tests/test_calyx_conversation.py tests/test_calyx_file_analysis.py
      - name: Lint Calyx analysis slice
        run: ruff check app/calyx_conversation tests/test_calyx_conversation.py tests/test_calyx_file_analysis.py
      - name: Verify router import
        run: python -c "from app.routers.calyx_core import router; paths={getattr(r, 'path', '') for r in router.routes}; assert '/api/calyx/query' in paths; assert '/api/calyx/dataset/upload-analyze' in paths; assert '/api/calyx/dataset/upload-report' in paths"
