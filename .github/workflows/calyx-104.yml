name: CALYX-104 Validation

on:
  pull_request:
    paths:
      - 'runtime/calyx_certification/cross_lane_integration.py'
      - 'tests/test_calyx_104_cross_lane_integration.py'
      - '.github/workflows/calyx-104.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: ${{ github.workspace }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - run: pip install pytest ruff
      - run: ruff check runtime/calyx_certification/cross_lane_integration.py tests/test_calyx_104_cross_lane_integration.py
      - run: pytest -q tests/test_calyx_104_cross_lane_integration.py
