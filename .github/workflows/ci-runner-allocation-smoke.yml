name: CI Runner Allocation Smoke

on:
  pull_request:
    paths:
      - ".github/workflows/ci-runner-allocation-smoke.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hosted-runner-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Prove hosted runner executed a real step
        run: echo "runner-step-executed"
