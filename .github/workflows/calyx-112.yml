name: CALYX-112 Validation

on:
  pull_request:
    paths:
      - "runtime/calyx_certification/audit_export.py"
      - "tests/test_calyx_112_audit_export.py"
      - ".github/workflows/calyx-112.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: .
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pytest ruff
      - run: ruff check runtime/calyx_certification/audit_export.py tests/test_calyx_112_audit_export.py
      - run: pytest -q tests/test_calyx_112_audit_export.py
