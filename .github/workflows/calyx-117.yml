name: CALYX-117 Validation

on:
  pull_request:
    paths:
      - "runtime/calyx_certification/preflight_execution_lock.py"
      - "tests/test_calyx_117_preflight_execution_lock.py"
      - ".github/workflows/calyx-117.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: .
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pytest ruff
      - run: ruff check runtime/calyx_certification/preflight_execution_lock.py tests/test_calyx_117_preflight_execution_lock.py
      - run: pytest -q tests/test_calyx_117_preflight_execution_lock.py
