name: CALYX-122 Validation

on:
  pull_request:
    paths:
      - "runtime/calyx_certification/post_release_verification.py"
      - "tests/test_calyx_122_post_release_verification.py"
      - ".github/workflows/calyx-122.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: .
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pytest ruff
      - run: ruff check runtime/calyx_certification/post_release_verification.py tests/test_calyx_122_post_release_verification.py
      - run: pytest -q tests/test_calyx_122_post_release_verification.py
